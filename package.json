{
  "name": "my-fullstack-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently -n backend,frontend -c yellow,cyan \"pnpm --filter backend run dev\" \"pnpm --filter frontend run dev\"",
    "build": "pnpm -r run build",
    "studio": "pnpm --filter database db:studio"
  },
  "packageManager": "pnpm@10.12.1",
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
